{
  "Define route loader": {
    "prefix": "rr:loader",
    "body": [
      "export const loader: LoaderFunction = async ({ params, request }) => {",
      "  $1",
      "}"
     ],
     "scope": "typescriptreact"
   },

   "Return JSON from loader": {
    "prefix": "rr:loader:json",
    "body": [
      "return json($1, {",
      "  status: $2,",
      "  headers: $3,",
      "})"
    ],
    "description": "return JSON with status and/or headers from the route's loader",
    "scope": "typescriptreact"
  }
}
